# Below command is to install updated version of pip
python.exe -m pip install --upgrade pip

# This below command is to see extra library/module in python
pip freeze

# Below command is to install tensorflow
pip install tensorflow

# This below command is to store requirements packages/module dependencies that need to be installed through pip
pip freeze > requirements.txt

# Install the dependencies from requirements.txt. This command instructs pip to install all the packages and versions listed in the
# requirements.txt file. It will automatically download and install the necessary packages from the Python Package Index (PyPI) or other
# specified sources.
pip install -r requirements.txt

# To verify use pip list
pip list

# Below command is to install matplotlib
pip install matplotlib

# %matplotlib inline is a magic command in Jupyter Notebook and JupyterLab that enables the inline display of plots generated by
# Matplotlib, a popular plotting library in Python. When you execute this command in a Jupyter Notebook or JupyterLab cell, it configures
# the environment to show the plots directly below the code cell, within the notebook interface.
%matplotlib inline